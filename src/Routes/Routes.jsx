import { createBrowserRouter } from "react-router-dom";

const router = createBrowserRouter([
    {
        path: '/',
        element: <div className="font-bold underline">Hello world</div>
    }
])

export default router;
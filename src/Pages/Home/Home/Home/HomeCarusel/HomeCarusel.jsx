import React from 'react';

const HomeCarusel = ({chefs}) => {
    console.log(chefs);
    return (
        <div>
            this page for carusel
        </div>
    );
};

export default HomeCarusel;